// import { fromJS } from 'immutable';
// import { makeSelectParkingLotDomain } from '../selectors';

// const selector = makeSelectParkingLotDomain();

describe('makeSelectParkingLotDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
