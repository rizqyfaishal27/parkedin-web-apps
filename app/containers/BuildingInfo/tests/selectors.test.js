// import { fromJS } from 'immutable';
// import { makeSelectBuildingInfoDomain } from '../selectors';

// const selector = makeSelectBuildingInfoDomain();

describe('makeSelectBuildingInfoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
