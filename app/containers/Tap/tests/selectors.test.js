// import { fromJS } from 'immutable';
// import { makeSelectTapDomain } from '../selectors';

// const selector = makeSelectTapDomain();

describe('makeSelectTapDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
