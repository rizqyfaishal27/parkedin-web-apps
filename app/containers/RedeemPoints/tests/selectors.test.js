// import { fromJS } from 'immutable';
// import { makeSelectRedeemPointsDomain } from '../selectors';

// const selector = makeSelectRedeemPointsDomain();

describe('makeSelectRedeemPointsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
