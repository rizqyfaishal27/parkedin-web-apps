// import { fromJS } from 'immutable';
// import { makeSelectRecommendationDomain } from '../selectors';

// const selector = makeSelectRecommendationDomain();

describe('makeSelectRecommendationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
