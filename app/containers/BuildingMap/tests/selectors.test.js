// import { fromJS } from 'immutable';
// import { makeSelectBuildingMapDomain } from '../selectors';

// const selector = makeSelectBuildingMapDomain();

describe('makeSelectBuildingMapDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
