// import { fromJS } from 'immutable';
// import { makeSelectPincodeDomain } from '../selectors';

// const selector = makeSelectPincodeDomain();

describe('makeSelectPincodeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
