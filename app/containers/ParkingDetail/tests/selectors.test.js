// import { fromJS } from 'immutable';
// import { makeSelectParkingDetailDomain } from '../selectors';

// const selector = makeSelectParkingDetailDomain();

describe('makeSelectParkingDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
