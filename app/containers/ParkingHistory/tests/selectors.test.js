// import { fromJS } from 'immutable';
// import { makeSelectParkingHistoryDomain } from '../selectors';

// const selector = makeSelectParkingHistoryDomain();

describe('makeSelectParkingHistoryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
